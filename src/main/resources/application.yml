spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/MeetyDB
    username: postgres
    password: admin
  #    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
